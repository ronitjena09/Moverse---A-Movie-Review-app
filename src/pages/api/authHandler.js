
// utils/authHandler.js
// This is a separate file for handling internal auth logic

export default async function authHandler(user) {
    // Perform your authentication logic here
    // For example, generate a token or handle session creation
    return { success: true, message: "User registered successfully", user };
  }
  